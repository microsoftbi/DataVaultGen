[project]
name = "data-vault-generator-backend"
version = "1.0.0"
description = "Data Vault Generator Backend"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["app*"]